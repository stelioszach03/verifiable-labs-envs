"""Preflight verification toolkit (Track A).

Pre-deploy + pre-training smoke tests. CPU-only; no GPU operations.
The scripts in this package are invoked manually before Stelios runs
``flyctl deploy`` or kicks off Phase 29.F + 30.F training.
"""
