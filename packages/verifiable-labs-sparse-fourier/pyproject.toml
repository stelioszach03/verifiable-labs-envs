[build-system]
requires = ["hatchling>=1.24,<1.27"]
build-backend = "hatchling.build"

[project]
name = "verifiable-labs-sparse-fourier"
version = "0.1.1"
description = "1D sparse Fourier recovery with OMP baseline + conformal σ̂"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "Stelios Zacharioudakis" }]
keywords = ["reinforcement-learning", "inverse-problems", "rlvr", "evaluation", "scientific-ml"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "verifiable-labs-envs-core>=0.1.1",
]

[project.urls]
Homepage = "https://github.com/verifiablelabs/verifiable-labs-envs"
Issues = "https://github.com/verifiablelabs/verifiable-labs-envs/issues"

# Verifiers-compatible entry point so `prime env install verifiable-labs-sparse-fourier` / the
# Prime Intellect Hub can discover load_environment() automatically.
[project.entry-points."verifiers.environments"]
sparse-fourier-recovery = "verifiable_labs_sparse_fourier:load_environment"

[tool.hatch.build.targets.wheel]
packages = ["src/verifiable_labs_sparse_fourier"]
